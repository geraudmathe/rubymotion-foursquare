$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'
require 'bubble-wrap'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'rubymotion foursquare'
  app.frameworks += ['CoreLocation', 'MapKit', 'AddressBook']
  app.provisioning_profile = "/Users/gmathe08/developpement/iOS_Team_Provisioning_Profile_.mobileprovision"
  #app.deployment_target = "4.2"
end
